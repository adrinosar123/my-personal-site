import React from "react"
import { Link } from "gatsby"

import Layout from "../components/Layout"
import MainContactSection from "../components/contactPage/MainContactSection"

export default () => {
  return (
    <Layout>
      <MainContactSection />
    </Layout>
  )
}